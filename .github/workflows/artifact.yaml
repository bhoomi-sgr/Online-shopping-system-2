name: Artifact Demos

on: push


jobs:
  build:
     runs-on: ubuntu-latest
     
     steps:
       - run: |
           mkdir temp
           echo "alpha" > tempt/alpha.html

       - uses: actions/upload-artifact@v4
         with:
           name: files
           path: temp/
         
